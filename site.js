const form=document.getElementById('brief-form');if(form){form.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form);const names={project:'Project / organization',location:'Location',application:'Application',question:'Decision to make',data:'Existing information',timing:'Timing and considerations'};let text='DWELL — PRELIMINARY CONSULTATION BRIEF\n\n';for(const [key,label] of Object.entries(names)){text+=label+'\n'+(data.get(key)||'Not specified')+'\n\n';}text+='Prepared locally. This brief has not been sent and does not book a consultation.\n';const url=URL.createObjectURL(new Blob([text],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='Dwell-project-brief.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);document.getElementById('brief-status').textContent='Your brief is ready to download. Nothing has been sent.';});}